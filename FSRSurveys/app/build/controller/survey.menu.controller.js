var survey;
(function (survey) {
    var MenuController = (function () {
        function MenuController() {
        }
        return MenuController;
    }());
    angular.module("survey")
        .controller("MenuController", MenuController);
})(survey || (survey = {}));
//# sourceMappingURL=survey.menu.controller.js.map